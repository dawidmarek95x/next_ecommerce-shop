query ProductsGetList($limit: Int, $offset: Int) {
	products(first: $limit, skip: $offset) {
		id
		name
		slug
		description
		price
		categories(first: 1) {
			name
			slug
		}
		images(first: 1) {
			url
		}
	}
	productsConnection {
		aggregate {
			count
		}
	}
}
