fragment Cart on Order {
	id
	userId
	email
	total
	stripeCheckoutId
	orderItems {
		id
		quantity
		total
		product {
			id
			name
			price
			categories {
				name
			}
			images {
				url
			}
		}
	}
	createdAt
}
